# Dependencies (root workspace + per-package if hoisting differs)
node_modules/

# Build outputs
server/dist/
client/dist/

# Vite / tooling
client/.vite/
*.tsbuildinfo

# Environment (secrets; optional template can be committed as .env.example)
.env
.env.*
!.env.example

# Logs & OS
*.log
.DS_Store
Thumbs.db
