{
  "name": "leumi-web",
  "private": true,
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c blue,magenta \"npm run dev -w leumi-web-server\" \"npm run dev -w leumi-web-client\"",
    "build": "npm run build -w leumi-web-server && npm run build -w leumi-web-client"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
